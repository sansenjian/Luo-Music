[package]
name = "audio-engine-decode"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
anyhow.workspace = true
ffmpeg-next = { workspace = true, optional = true }

[features]
default = []
ffmpeg = ["dep:ffmpeg-next"]
opus = []
